# AIによる資料生成・自動分類を使う場合に設定します（未設定でもアプリは動きます）
ANTHROPIC_API_KEY=sk-ant-...

# 使うモデル（省略時は claude-opus-5）
# CLAUDE_MODEL=claude-opus-5

# 待ち受けポート（省略時は 3000）
# PORT=3000

# データ保存先（省略時はリポジトリ内の data/）
# DATA_DIR=./data

# 添付ファイルの上限（バイト / 件数）
# MAX_FILE_SIZE=26214400
# MAX_FILES=12
